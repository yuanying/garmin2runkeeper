<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:users, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:users, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:user) %></h2>
    <div class="inner">
      <% form_for :user, url(:users, :create), :class => :form do |f| %>
        <%= partial "users/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
