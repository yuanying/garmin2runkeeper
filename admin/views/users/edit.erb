<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:users, :index) %></li>
      <li><%= link_to pat(:new), url(:users, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:users, :edit, :id => @user.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:user) %></h2>
    <div class="inner">
      <% form_for :user, url(:users, :update, :id => @user.id), :method => :put, :class => :form do |f| %>
        <%= partial "users/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
